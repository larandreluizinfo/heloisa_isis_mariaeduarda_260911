# Regras para o agente

- Toda alteração realizada neste repositório **deve** ser commitada e pushada para o GitHub.
- Ao finalizar qualquer tarefa, faça o `git add`, `git commit` com mensagem descritiva e `git push`.